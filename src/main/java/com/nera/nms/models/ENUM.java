package com.nera.nms.models;

public final class ENUM {
	public enum StatusPlaybook {
		DRAFT,
		NEW,
		APPROVED,
		REJECTED
	}

	public enum PlaybookInputType {
		FILE,
		TEXT
	}

	public enum PlaybookOutputType {
		FILE,
		TEXT
	}
}
